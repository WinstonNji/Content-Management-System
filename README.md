# Content-Management-System
Text classifier trained on the Jigsaw Toxic Comment Classification dataset to detect toxic, flagged, and clean content. Built a progression of architectures from a baseline LSTM to stacked LSTM with funnel structure, then swapped random embeddings for pretrained GloVe (840B, 300d) vectors to bring in semantic meaning.
